[phases.setup]
nixPkgsArchive = "default"
nixPkgs = ["php84", "php84Extensions.gd", "php84Extensions.pdo", "php84Extensions.pdo_sqlite", "php84Extensions.openssl"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."